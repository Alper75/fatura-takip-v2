const fs = require('fs');
const path = require('path');

const p = path.resolve('src/sections/AlisFaturaDrawer.tsx');
let content = fs.readFileSync(p, 'utf8');

// 1. Update State variables
content = content.replace(
  "const [uploadedFile, setUploadedFile] = useState<UploadedFile | null>(null);",
  "const [uploadedFiles, setUploadedFiles] = useState<UploadedFile[]>([]);\n  const [scanProgress, setScanProgress] = useState<{ current: number, total: number } | null>(null);"
);

// 2. Update handleClose
content = content.replace(
  "setUploadedFile(null);",
  "setUploadedFiles([]);\n    setScanProgress(null);"
);

// 3. Update dosyaBase64 logic in handleSubmit
content = content.replace(
  "dosyaBase64: uploadedFile?.base64,",
  "dosyaBase64: f.data.dosyaBase64 || \"\","
);
content = content.replace(
  "dosyaAdi: uploadedFile?.name",
  "dosyaAdi: f.data.dosyaAdi || \"\","
);

// 4. Replace handleFileChange & processImageFile with compressImage & processFiles
content = content.replace(
  /const handleFileChange = \(e: React\.ChangeEvent<HTMLInputElement>\) => {[\s\S]*?reader\.readAsDataURL\(file\);\s*};/,
  "const compressImage = (file: File): Promise<string> => {\n    return new Promise((resolve, reject) => {\n      const reader = new FileReader();\n      reader.onload = (e) => {\n        const img = new Image();\n        img.onload = () => {\n          const canvas = document.createElement('canvas');\n          let width = img.width;\n          let height = img.height;\n          const MAX = 1600;\n\n          if (width > height) {\n            if (width > MAX) { height = Math.round(height * (MAX / width)); width = MAX; }\n          } else {\n            if (height > MAX) { width = Math.round(width * (MAX / height)); height = MAX; }\n          }\n\n          canvas.width = width; canvas.height = height;\n          const ctx = canvas.getContext('2d');\n          ctx?.drawImage(img, 0, 0, width, height);\n          resolve(canvas.toDataURL('image/jpeg', 0.7));\n        };\n        img.onerror = reject;\n        img.src = e.target?.result as string;\n      };\n      reader.onerror = reject;\n      reader.readAsDataURL(file);\n    });\n  };\n\n  const handleFileChange = async (e: React.ChangeEvent<HTMLInputElement>) => {\n    if (e.target.files && e.target.files.length > 0) {\n      await processFiles(Array.from(e.target.files));\n    }\n  };\n\n  const processFiles = async (files: File[]) => {\n    setIsScanning(true);\n    try {\n      const newFiles: UploadedFile[] = [];\n      for (const file of files) {\n        if (file.type.startsWith('image/')) {\n          const b64 = await compressImage(file);\n          newFiles.push({ base64: b64, mimeType: 'image/jpeg', name: file.name });\n        } else {\n          const b64 = await new Promise<string>((resolve, reject) => {\n            const reader = new FileReader();\n            reader.onload = (e) => resolve(e.target?.result as string);\n            reader.onerror = reject;\n            reader.readAsDataURL(file);\n          });\n          newFiles.push({ base64: b64, mimeType: file.type, name: file.name });\n        }\n      }\n      setUploadedFiles(prev => [...prev, ...newFiles]);\n      setAiAddedCount(0);\n    } catch (err) {\n      toast.error('Dosya islenirken hata olustu.');\n    } finally {\n      setIsScanning(false);\n    }\n  };"
);

// 5. Update scanImage
content = content.replace(
  /const scanImage = async \(\) => {[\s\S]*?if \(!parsed\.hata\) {[\s\S]*?setAiAddedCount\(prev => prev \+ newForms\.length\);\s*}\s*} catch \(err: any\) {[\s\S]*?}\s*\};\s*return \(/,
  "const scanImage = async () => {\n    if (uploadedFiles.length === 0) return;\n\n    setIsScanning(true);\n    let totalAdded = 0;\n    \n    let apiKey = import.meta.env.VITE_GEMINI_API_KEY || '';\n    let aiModel = 'gemini-2.5-flash';\n    \n    try {\n      const keyRes = await apiFetch('/api/settings/gemini_api_key');\n      if (keyRes && keyRes.value) apiKey = keyRes.value;\n      const modelRes = await apiFetch('/api/settings/gemini_model');\n      if (modelRes && modelRes.value) aiModel = modelRes.value;\n    } catch (keyErr) {}\n\n    if (!apiKey) {\n      toast.error('Yapay zeka anahtari bulunamadi.');\n      setIsScanning(false);\n      return;\n    }\n\n    const safeModelName = aiModel ? aiModel.trim() : 'gemini-2.5-flash';\n\n    const [settingsRes, bankaRes] = await Promise.all([\n      apiFetch('/api/settings/luca_kdv_ayarlari').catch(() => null),\n      apiFetch('/api/banka-hesaplari').catch(() => null)\n    ]);\n    \n    let kdvSettings: any = {};\n    if (settingsRes?.success && settingsRes.value) {\n      try { kdvSettings = JSON.parse(settingsRes.value); } catch(e){}\n    }\n    const bankaHesaplari: any[] = bankaRes?.success ? bankaRes.data : [];\n\n    for (let i = 0; i < uploadedFiles.length; i++) {\n      const file = uploadedFiles[i];\n      setScanProgress({ current: i + 1, total: uploadedFiles.length });\n      \n      try {\n        const rawBase64 = file.base64.split(',')[1];\n        \n        const prompt = \"Bu dosyada BIRDEN FAZLA ayri fis veya fatura olabilir. Veya TEK BIR fiste/faturada BIRDEN FAZLA FARKLI KDV orani (Orn: %1, %10, %20) olabilir.\\n  Lutfen buldugun TUM fis/faturalari cikar ve asagidaki JSON DIZISI formatinda dondur.\\n\\n  ONEMLI KURAL: Eger tek bir fiste/faturada birden fazla KDV orani varsa (Orn: bazi urunler %1, bazilari %20), LUTFEN her bir KDV oraninin toplam tutarini (kdv dahil) ayri birer JSON objesi (ayri bir fatura kaydi) olarak diziye ekle! Fatura no, tarih ve satici adi ayni kalsin, sadece tutar, malHizmetAdi ('... %20 KDV\\\\'li urunler' vb.) ve kdv_orani farkli olsun.\\n  ONEMLI KURAL 2: Eger bu bir akaryakit fisi/faturasi ise, fatura uzerinde yazan ARAC PLAKASINI mutlaka 'plate' alanina yaz.\\n  ONEMLI KURAL 3: Yemek (Restoran/Lokanta), akaryakit, konaklama, kirtasiye, ofis tuketim, market gibi sirket ici genel harcama fisleri KESINLIKLE 7'li gider hesaplarina (Orn: 770 Genel Yonetim Giderleri) atilmalidir. 153 Ticari Mallar hesabi SADECE satmak amaciyla alinan urunler icin kullanilir. Fisin turune dikkat ederek en uygun hesabi sec.\\n  ONEMLI KURAL 4: Fis/fatura uzerindeki odeme tipini analiz et. Eger slip veya fis uzerinde '**** 1104', 'Kredi Karti', 'Banka Karti' gibi kredi karti odemesine dair bir ibare varsa 'odeme_sekli': 'KREDI_KARTI' yap ve kartin son 4 hanesini 'kredi_karti_son4' alanina yaz (orn: '1104'). Eger nakit odenmisse veya belli degilse 'odeme_sekli': 'NAKIT' yap.\\n\\n  Asagidaki LUCA HESAP PLANI listesinden bu faturanin aciklamasina/turune en uygun 'kod'u secerek 'muhasebe_kodu' alanina yaz:\\n  \" + lucaAccounts.map(a => a.kod + \": \" + a.ad).join('\\n') + \"\\n\\n  SADECE JSON dondur:\\n  {\\n    \\\"faturalar\\\": [\\n      {\\n        \\\"tedarikciAdi\\\": \\\"firma/satici adi\\\",\\n        \\\"tedarikciVkn\\\": \\\"VKN veya T.C.\\\",\\n        \\\"faturaNo\\\": \\\"belge no\\\",\\n        \\\"malHizmetAdi\\\": \\\"urun ozeti veya ... %KDV'li urunler\\\",\\n        \\\"faturaTarihi\\\": \\\"YYYY-MM-DD\\\",\\n        \\\"tutar\\\": \\\"120.50\\\",\\n        \\\"tutar_tur\\\": \\\"dahil\\\",\\n        \\\"kdv_orani\\\": \\"18\\",\\n        \\\"tevkifat_orani\\\": \\"0\\",\\n        \\\"stopaj_orani\\\": \\"0\\",\\n        \\\"aciklama\\\": \\"\\",\\n        \\\"muhasebe_kodu\\\": \\"Yukaridaki listeden secilen en uygun hesap kodu\\",\\n        \\\"plate\\": \\"Eger varsa arac plakasi (34ABC123 formatinda)\\",\\n        \\\"odeme_sekli\\": \\"KREDI_KARTI veya NAKIT\\",\\n        \\\"kredi_karti_son4\\": \\"Eger kartsa slipteki son 4 hane, yoksa bos birak\\"\\n      }\\n    ]\\n  }\";\n\n        const aiResponse = await fetch(\"https://generativelanguage.googleapis.com/v1beta/models/\" + safeModelName + \":generateContent?key=\" + apiKey, {\n          method: 'POST',\n          headers: { 'Content-Type': 'application/json' },\n          body: JSON.stringify({\n            contents: [{\n              parts: [\n                { text: prompt },\n                { inline_data: { mime_type: file.mimeType, data: rawBase64 } }\n              ]\n            }],\n            generationConfig: { responseMimeType: \"application/json\" }\n          })\n        });\n\n        const data = await aiResponse.json();\n        if (data.error) throw new Error(data.error.message || 'API Hatasi');\n\n        const text = data.candidates?.[0]?.content?.parts?.[0]?.text || '';\n        const clean = text.replace(/```json|```/g, '').trim();\n        const parsed = JSON.parse(clean);\n\n        if (!parsed.hata) {\n          const fList = parsed.faturalar ? parsed.faturalar : (Array.isArray(parsed) ? parsed : [parsed]);\n          \n          const newForms: FormEntry[] = fList.map((f: any, idx: number) => {\n            const isFuel = /akaryakit|yakit|benzin|motorin/i.test(f.malHizmetAdi);\n            const isVehicleExpense = /bakim|onarim|otopark|yikama/i.test(f.malHizmetAdi);\n            const needsPlate = isFuel || isVehicleExpense;\n            \n            let finalKdv = String(f.kdv_orani);\n            if (kdvSettings[f.muhasebe_kodu]) finalKdv = String(kdvSettings[f.muhasebe_kodu]);\n            if (!KDV_ORANLARI.includes(finalKdv)) finalKdv = '18';\n\n            let selectedAccount = bankaHesaplari[0]?.id || '';\n            let odemeDurumu = f.odeme_sekli === 'KREDI_KARTI' ? 'odendi' : 'odenmedi';\n            let odemeDekontu = '';\n\n            if (f.odeme_sekli === 'KREDI_KARTI' && f.kredi_karti_son4) {\n              const matchedBank = bankaHesaplari.find(b => \n                b.kart_son_dort && String(b.kart_son_dort).includes(String(f.kredi_karti_son4))\n              );\n              if (matchedBank) {\n                selectedAccount = matchedBank.id;\n                odemeDekontu = matchedBank.id;\n              }\n            }\n            if (f.odeme_sekli === 'NAKIT') {\n              const nakitHesap = bankaHesaplari.find(b => String(b.hesap_adi).toLowerCase().includes('kasa'));\n              if (nakitHesap) {\n                selectedAccount = nakitHesap.id;\n                odemeDekontu = nakitHesap.id;\n                odemeDurumu = 'odendi';\n              }\n            }\n\n            return {\n              id: Date.now() + idx + Math.random(),\n              data: {\n                faturaNo: f.faturaNo || '',\n                faturaTarihi: f.faturaTarihi || new Date().toISOString().split('T')[0],\n                vadeTarihi: f.faturaTarihi || '',\n                tedarikciAdi: f.tedarikciAdi || '',\n                tedarikciVkn: f.tedarikciVkn || '',\n                malHizmetAdi: f.malHizmetAdi || '',\n                toplamTutar: f.tutar || '',\n                kdvOrani: finalKdv,\n                tevkifatOrani: TEVKIFAT_ORANLARI.includes(String(f.tevkifat_orani)) ? String(f.tevkifat_orani) : '0',\n                stopajOrani: String(f.stopaj_orani || '0'),\n                aciklama: f.aciklama || '',\n                muhasebeKodu: f.muhasebe_kodu || '',\n                vehiclePlate: needsPlate ? (f.plate || '') : '',\n                odemeDurumu: odemeDurumu,\n                odemeDekontu: odemeDekontu,\n                dosyaBase64: file.base64,\n                dosyaAdi: file.name\n              },\n              tutarTuru: (f.tutar_tur === 'haric' || f.tutar_tur === 'hariç') ? 'haric' : 'dahil',\n              errors: {}\n            };\n          });\n\n          setForms(prev => {\n            const hasEmptyInitial = prev.length === 1 && !prev[0].data.faturaNo && !prev[0].data.tedarikciAdi;\n            const currentForms = hasEmptyInitial ? [] : [...prev];\n            return [...currentForms, ...newForms];\n          });\n          \n          totalAdded += newForms.length;\n          setAiAddedCount(prev => prev + newForms.length);\n        }\n      } catch (err: any) {\n        toast.error(file.name + \" okunamadi: \" + (err.message || 'Bilinmeyen Hata'));\n      }\n    }\n\n    setScanProgress(null);\n    setIsScanning(false);\n    if (totalAdded > 0) toast.success(\"AI tarafindan toplam \" + totalAdded + \" adet sonuc cikarildi!\");\n  };\n\n  return ("
);

// 6. Update UI
// Replace the block from <div className="space-y-3"> to </form>
const uiStart = content.indexOf('<div className="space-y-3">');
const uiEnd = content.indexOf('<form onSubmit={handleSubmit} className="space-y-6">');
if (uiStart > -1 && uiEnd > -1) {
  content = content.substring(0, uiStart) + 
  "<div className=\"space-y-3\">\n            {uploadedFiles.length === 0 ? (\n              <div\n                className={cn(\n                  \"border-2 border-dashed rounded-xl p-8 text-center cursor-pointer transition-colors relative\",\n                  isDragging ? \"border-indigo-500 bg-indigo-50/50\" : \"border-slate-200 hover:border-indigo-400 hover:bg-slate-50\"\n                )}\n                onDragOver={(e) => { e.preventDefault(); setIsDragging(true); }}\n                onDragLeave={() => setIsDragging(false)}\n                onDrop={(e) => {\n                  e.preventDefault();\n                  setIsDragging(false);\n                  if (e.dataTransfer.files?.length > 0) processFiles(Array.from(e.dataTransfer.files));\n                }}\n                onClick={() => fileInputRef.current?.click()}\n              >\n                <input type=\"file\" ref={fileInputRef} className=\"hidden\" accept=\"image/*,application/pdf\" multiple onChange={handleFileChange} />\n                <div className=\"w-12 h-12 rounded-full bg-slate-100 flex items-center justify-center mx-auto mb-3\">\n                  <FileText className=\"w-6 h-6 text-slate-500\" />\n                </div>\n                <h4 className=\"font-semibold text-slate-900\">PDF veya Resim (Fatura/Fis) Yukleyin</h4>\n                <p className=\"text-sm text-slate-500 mt-1\">Gelistirilmis AI modeli birden fazla PDF ve resmi aninda okur!</p>\n              </div>\n            ) : (\n              <div className=\"space-y-4\">\n                <div className=\"grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-3\">\n                  {uploadedFiles.map((file, idx) => (\n                    <div key={idx} className=\"relative border rounded-lg overflow-hidden bg-slate-50 group aspect-square flex items-center justify-center\">\n                      {file.mimeType === 'application/pdf' ? (\n                        <div className=\"flex flex-col items-center justify-center p-2 text-center\">\n                          <FileText className=\"w-8 h-8 text-red-400 mb-1\" />\n                          <p className=\"text-xs font-medium text-slate-600 truncate w-full px-2\">{file.name}</p>\n                        </div>\n                      ) : (\n                        <img src={file.base64} alt={file.name} className=\"w-full h-full object-cover\" />\n                      )}\n                      \n                      <div className=\"absolute top-1 right-1 opacity-0 group-hover:opacity-100 transition-opacity\">\n                        <Button type=\"button\" size=\"icon\" variant=\"destructive\" className=\"h-6 w-6 rounded-full shadow-sm\" onClick={(e) => {\n                          e.stopPropagation();\n                          setUploadedFiles(prev => prev.filter((_, i) => i !== idx));\n                        }}>\n                          <X className=\"w-3 h-3\" />\n                        </Button>\n                      </div>\n                    </div>\n                  ))}\n                  \n                  {/* Add more button */}\n                  <div \n                    onClick={() => fileInputRef.current?.click()}\n                    className=\"border-2 border-dashed border-slate-200 rounded-lg flex flex-col items-center justify-center cursor-pointer hover:border-indigo-400 hover:bg-slate-50 transition-colors aspect-square\"\n                  >\n                    <Plus className=\"w-6 h-6 text-slate-400 mb-1\" />\n                    <span className=\"text-xs text-slate-500 font-medium\">Dosya Ekle</span>\n                    <input type=\"file\" ref={fileInputRef} className=\"hidden\" accept=\"image/*,application/pdf\" multiple onChange={handleFileChange} />\n                  </div>\n                </div>\n\n                {aiAddedCount === 0 && (\n                  <div className=\"flex justify-center\">\n                    <Button\n                      type=\"button\"\n                      onClick={scanImage}\n                      disabled={isScanning}\n                      className=\"bg-indigo-600 hover:bg-indigo-700 text-white shadow-lg font-semibold rounded-full px-8 py-6 text-lg\"\n                    >\n                      {isScanning ? (\n                        <>\n                          <Loader2 className=\"w-5 h-5 mr-3 animate-spin\" />\n                          {scanProgress ? \"Diziliyor (\" + scanProgress.current + \"/\" + scanProgress.total + \")...\" : \"Analiz Ediliyor...\"}\n                        </>\n                      ) : (\n                        <>\n                          <Sparkles className=\"w-5 h-5 mr-3\" />\n                          Tümünü Analiz Et ({uploadedFiles.length} Dosya)\n                        </>\n                      )}\n                    </Button>\n                  </div>\n                )}\n              </div>\n            )}\n\n            {aiAddedCount > 0 && (\n              <div className=\"bg-emerald-50 border border-emerald-100 rounded-lg p-4 flex items-start gap-3\">\n                <CheckCircle2 className=\"w-6 h-6 text-emerald-600 shrink-0\" />\n                <div className=\"text-sm text-emerald-900\">\n                  <span className=\"font-semibold text-base block mb-1\">Tarama tamamlandi!</span> \n                  Yuklediginiz dosyalardan toplam <b>{aiAddedCount} adet</b> fis/fatura cikarildi.\n                </div>\n              </div>\n            )}\n          </div>\n          \n          " + content.substring(uiEnd);
}

fs.writeFileSync(p, content, 'utf8');
console.log('AlisFaturaDrawer updated successfully.');
